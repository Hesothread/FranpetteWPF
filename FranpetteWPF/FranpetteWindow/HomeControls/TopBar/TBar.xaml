<UserControl x:Class="FranpetteWPF.FranpetteWindow.HomeControls.TopBar.TBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TopButton" TargetType="TextBlock">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Verdana"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="LeftButton" TargetType="TextBlock" BasedOn="{StaticResource TopButton}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RightButton" TargetType="TextBlock" BasedOn="{StaticResource TopButton}">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="LightGreen"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Name="TBarGrid" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Style="{StaticResource LeftButton}" Grid.Column="0" Text="🌍 Français"/>
        <TextBlock Style="{StaticResource LeftButton}" Grid.Column="1" Text="Options"/>
        <TextBlock Style="{StaticResource LeftButton}" Grid.Column="2" Text="Plus"/>
        
        <TextBlock Style="{StaticResource RightButton}" Grid.Column="4" Text="Mon compte ⯆"/>
        
        <Grid Grid.Row="1" Grid.ColumnSpan="5" Background="#444">
            
        </Grid>
    </Grid>
</UserControl>
